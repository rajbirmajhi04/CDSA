#include <iostream>
#include <vector>
#include <array>
using namespace std;

int main()
{
    // // Problem 1
    // int n, q;
    // cin >> n >> q;

    // // read the arrays
    // vector <vector <int>> arr(n); // decalre vector of vectors
    // for(int i = 0; i < n; i++){
    //     int k;
    //     cin >> k;

    //     arr[i].resize(k); // resize the ith vector to hold k elements
    //     for(int j = 0; j < k; j++){
    //         cin >> arr[i][j];
    //     }
    // }

    // // process the queries
    // for(int q_num = 0; q_num < q; q_num++){
    //     int i, j;
    //     cin >> i >> j;
    //     cout << arr[i][j] << endl;
    // }

    // // Problem 2
    // string a, b;
    // cin >> a >> b;

    // cout << a.length() << " " << b.length() << endl;
    // cout << a + b << endl;
    // string modifiedA = a, modifiedB = b;
    // swap(modifiedA[0], modifiedB[0]);
    // cout << modifiedA << " " << modifiedB << endl;


    
    return 0;
}